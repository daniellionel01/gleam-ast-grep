id: stdlib_prefer_string_is_empty
language: gleam
severity: warning
message: Use string.is_empty instead of counting graphemes
rule:
  any:
    - pattern: string.length($STRING) == 0
fix: string.is_empty($STRING)
